# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/dreamkast.cloudnativedays.jp_reviewapps.yaml
- bases/dreamkast.cloudnativedays.jp_reviewappmanagers.yaml
- bases/dreamkast.cloudnativedays.jp_applicationtemplates.yaml
- bases/dreamkast.cloudnativedays.jp_manifeststemplates.yaml
- bases/dreamkast.cloudnativedays.jp_jobtemplates.yaml
- bases/dreamkast.cloudnativedays.jp_pullrequests.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_reviewapps.yaml
#- patches/webhook_in_reviewappmanagers.yaml
#- patches/webhook_in_applicationtemplates.yaml
#- patches/webhook_in_manifeststemplates.yaml
#- patches/webhook_in_jobtemplates.yaml
#- patches/webhook_in_pullrequestcaches.yaml
#- patches/webhook_in_pullrequests.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable webhook, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_reviewapps.yaml
#- patches/cainjection_in_reviewappmanagers.yaml
#- patches/cainjection_in_applicationtemplates.yaml
#- patches/cainjection_in_manifeststemplates.yaml
#- patches/cainjection_in_jobtemplates.yaml
#- patches/cainjection_in_pullrequestcaches.yaml
#- patches/cainjection_in_pullrequests.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
